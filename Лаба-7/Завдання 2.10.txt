#include <iostream>
#include <string>
#include<cmath>
using namespace std;

int main()
{
    int a;
do{
cout<<"Enter a positive number that isn't 1: ";
cin>>a;
    } while (a <= 1 or a>210);

cout << '+';
for(int i = 0; i < a; i++)
cout << '-';
cout << '+' << endl;
for(int i = 0; i < a; i++) {
cout << '|';
for(int j = 0; j < a; j++)
cout << ' ';
cout << '|' << endl;
}
cout << '+';
for(int i = 0; i < a; i++)
cout << '-';
cout << '+' << endl;
return 0;
}